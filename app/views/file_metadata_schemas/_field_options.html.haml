#loading

- case field_type
- when nil || ""
    Select a field type to specify options
- when "DateTime"
    Specify format of date and time (not reqd for metadata)
- when "Date"
    Specify format of date and (not reqd for metadata)
- when "Char"
    Regex for validation (default /./)
    \/TEXTFIELD/
- when "String"
    Length of String
- when "Text"
    Length of text field
- when "Integer"
    Range of integers (min, max, specified)
- when "Float"
    Display (dp, sig fig)
- when "Multi"
    Options (selected)
- when "Radio"
    Options (selected)
- when "Select"
    Options (selected)
- when "Blob"
    Max Size
- else
    Unrecognised field type! Take a screenshot and submit a bug report
