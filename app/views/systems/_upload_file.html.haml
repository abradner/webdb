.hidden.uploader
  = form_for [@system, file_type, RawStorageContainer.new], :html => {:multipart => true} do |f|
    = f.file_field :raw_file
    = f.submit "Upload File", :class => "blue basic_field"
